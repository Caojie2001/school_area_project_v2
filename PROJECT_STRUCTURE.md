# 项目结构详细说明

本文档详细说明了高校建筑面积缺口测算系统 V2 的完整项目结构。

## 📁 目录树

```
school_area_project_250922/
│
├── 🔧 配置与核心文件
│   ├── config/                          # 后端配置文件目录
│   │   ├── database.js                  # MySQL数据库连接池配置
│   │   ├── authService.js               # 用户认证服务
│   │   ├── dataService.js               # 数据持久化服务（核心，2600+行）
│   │   ├── routes.js                    # 路由配置
│   │   ├── appConfig.js                 # 应用配置
│   │   └── certs/                       # SSL证书目录
│   │       ├── cert.pem                 # SSL证书
│   │       ├── key.pem                  # SSL私钥
│   │       ├── generate_cert.sh         # 证书生成脚本
│   │       └── README.md                # 证书说明文档
│   │
│   ├── server.js                        # Express主服务器（5730行）
│   ├── package.json                     # NPM依赖配置
│   ├── package-lock.json                # 依赖版本锁定
│   ├── .env                             # 环境变量（不提交到Git）
│   ├── .env.example                     # 环境变量模板
│   ├── .gitignore                       # Git忽略文件
│   ├── Dockerfile                       # Docker容器配置
│   └── install.sh                       # 安装脚本
│
├── 🌐 前端资源
│   └── public/                          # 前端静态资源根目录
│       ├── index.html                   # 主页（登录后首页）
│       ├── login.html                   # 登录页面
│       │
│       ├── html/                        # 页面模块目录
│       │   ├── data-entry.html          # 数据录入（新版，当前使用）
│       │   ├── data-management.html     # 历史记录管理
│       │   ├── statistics.html          # 统计分析页面
│       │   ├── calculation-standards.html # 标准配置管理（管理员）
│       │   └── user-management.html     # 用户管理（管理员）
│       │
│       ├── js/                          # JavaScript模块目录
│       │   ├── main.js                  # 主应用入口
│       │   ├── auth.js                  # 认证模块
│       │   ├── api.js                   # API调用封装
│       │   ├── utils.js                 # 工具函数库
│       │   ├── dataEntry.js             # 数据录入逻辑
│       │   ├── dataManagement.js        # 数据管理逻辑
│       │   ├── statistics.js            # 统计分析逻辑
│       │   ├── calculationStandards.js  # 标准管理逻辑
│       │   ├── userManagement.js        # 用户管理逻辑
│       │   ├── componentManager.js      # 组件管理器
│       │   └── progressManager.js       # 进度管理器
│       │
│       ├── css/                         # 样式文件目录（模块化）
│       │   ├── main.css                 # 主样式入口
│       │   ├── base.css                 # 基础样式和重置
│       │   ├── layout.css               # 布局和栅格系统
│       │   ├── components.css           # UI组件样式
│       │   ├── forms.css                # 表单样式
│       │   ├── tables.css               # 表格样式
│       │   ├── frozen-table.css         # 冻结表格样式
│       │   ├── results.css              # 结果展示样式
│       │   ├── responsive.css           # 响应式媒体查询
│       │   └── student-fields.css       # 学生字段特定样式
│       │
│       ├── components/                  # 可复用HTML组件
│       │   ├── page-header.html         # 页面头部组件
│       │   ├── sidebar.html             # 侧边栏组件
│       │   └── page-template.html       # 页面模板
│       │
│       └── assets/                      # 静态资源（图片、图标等）
│
├── 🛠️ 工具脚本
│   └── scripts/                         # 脚本工具目录
│       ├── db.sql                       # 数据库初始化脚本（10000+行）
│       └── seed_users.js                # 批量创建用户脚本
│
├── 🧪 测试
│   └── test/                            # 测试目录
│       ├── README.md                    # 测试文档（232行）
│       ├── QUICK_REFERENCE.md           # 测试快速参考
│       ├── run_all_tests.sh             # 运行所有测试（主脚本）⭐
│       └── test_security.sh             # 安全功能测试（Helmet + Rate Limiting）
│
├── 📦 数据与备份
│   ├── data/                            # 数据文件目录
│   │   └── 原型/                        # 原型设计文件
│   │
│   ├── backups/                         # 备份文件目录（已整理）
│   │   ├── README.md                    # 备份说明文档
│   │   ├── database/                    # 数据库备份
│   │   │   ├── backup_20250905T052821/  # 2025年9月5日备份
│   │   │   └── backup_20250905T052857/  # 2025年9月5日备份
│   │   └── frontend_old/                # 前端旧版本备份（已废弃）
│   │       ├── cookies.txt
│   │       ├── index_original.html
│   │       ├── css_backup_20250814_135311/
│   │       ├── js_backup_20250814_135325/
│   │       └── [其他前端备份文件]
│   │
│   ├── logs/                            # 运行日志目录
│   │   ├── combined.log                 # 综合日志
│   │   └── error.log                    # 错误日志
│   │
│   └── output/                          # 输出文件目录（导出的Excel等）
│
├── 📄 文档
│   ├── README.md                        # 项目说明文档（主文档）
│   └── PROJECT_STRUCTURE.md             # 本文件 - 项目结构详解
│
└── ⚙️ 配置文件
    ├── .env                             # 环境变量（不提交）
    ├── .env.example                     # 环境变量模板
    ├── .gitignore                       # Git忽略规则
    ├── .dockerignore                    # Docker忽略规则
    └── Dockerfile                       # Docker镜像配置
```

---

## 📝 核心文件详细说明

### 后端核心

#### `server.js` (5730行)
**功能**: Express应用主服务器
**关键内容**:
- Express框架初始化
- 安全中间件配置（Helmet + Rate Limiting）
- Session会话管理
- 路由定义（100+个API端点）
- Excel生成和导出逻辑
- 在线计算功能
- 错误处理中间件

**安全功能** (2025-10-22新增):
```javascript
// Helmet 安全头
app.use(helmet({
    contentSecurityPolicy: { ... },
    crossOriginEmbedderPolicy: false,
    crossOriginResourcePolicy: { policy: "cross-origin" }
}));

// 5级限流策略
- loginLimiter: 5次/15分钟（防暴力破解）
- downloadLimiter: 10次/分钟（防数据爬取）
- calculateLimiter: 20次/分钟（防资源滥用）
- batchLimiter: 5次/分钟（防批量操作滥用）
- apiLimiter: 100次/15分钟（通用API限流）
```

---

#### `config/database.js`
**功能**: MySQL数据库连接配置
**内容**:
- 连接池配置
- 连接参数（从.env读取）
- 连接测试和错误处理
- 字符集设置（utf8mb4）

---

#### `config/authService.js`
**功能**: 用户认证服务
**提供的方法**:
- `loginUser(username, password)` - 用户登录验证
- `createUser(userData)` - 创建新用户
- `updateUserStatus(userId, isEnabled)` - 更新用户状态
- `getAllUsers()` - 获取所有用户
- `deleteUser(userId)` - 删除用户
- `changePassword(userId, newPassword)` - 修改密码

**安全特性**:
- BCrypt密码加密（10轮加盐）
- 密码强度验证
- Session管理

---

#### `config/dataService.js` (2600+行)
**功能**: 数据持久化核心服务
**核心职责**:
- 学校数据的增删改查
- 77个字段的数据映射和验证
- 计算历史记录管理
- 特殊补助数据处理
- 标准配置管理（基础标准、补贴标准）
- 规划学生数管理
- 现有面积预设管理

**关键方法**:
```javascript
// 保存学校记录（核心方法）
saveSchoolRecord(data)

// 获取学校记录列表
getSchoolRecords(filters)

// 通过ID获取单条记录
getSchoolRecordById(id)

// 删除记录
deleteSchoolRecord(id)

// 标准管理
getBaselineAreas()
updateBaselineArea(id, value)
getSubsidizedAreaStandards()

// 规划学生数
getPlannedStudents(schoolName, year)
savePlannedStudents(data)
```

**数据验证**:
- 数值规范化（parseFloat处理）
- 字段完整性检查
- 业务逻辑验证

---

### 前端核心

#### `public/js/api.js`
**功能**: 后端API调用封装
**提供的方法**:
```javascript
// 通用HTTP方法
apiGet(url)
apiPost(url, data)
apiPut(url, data)
apiDelete(url)

// 业务API
login(username, password)
logout()
saveSchoolData(data)
getSchoolData(filters)
downloadRecord(id)
onlineCalculate(data)
```

**特性**:
- 统一错误处理
- 自动Session管理
- 响应数据标准化
- 修复：null检查防止空指针错误

---

#### `public/js/dataEntry.js`
**功能**: 数据录入页面逻辑
**核心功能**:
- 学校和年份选择
- 规划学生数加载
- 面积数据录入（7大用房类型 × 7个维度）
- 实时计算（应配、汇总、缺口）
- 特殊补助管理
- 表单验证
- 数据提交和保存

**关键改进** (v2.1.0):
- 学校选择自动重置表单
- 保留学校选择，清空规划表和结果
- 优化交互体验

---

#### `public/js/dataManagement.js`
**功能**: 历史记录管理
**核心功能**:
- 记录列表展示
- 筛选和搜索
- 记录详情查看
- Excel下载
- 批量操作
- 记录删除

---

#### `public/js/statistics.js`
**功能**: 统计分析模块
**核心功能**:
- 总体概览统计
- 按院校类型分析
- 按用房类型分析
- 年度趋势分析
- 图表可视化（Chart.js）
- 缺口预警

---

### 数据库脚本

#### `scripts/db.sql` (10000+行)
**功能**: 数据库初始化和数据预置
**包含内容**:

1. **数据库和表结构创建**:
   - `users` - 用户表
   - `calculation_history` - 计算历史（77个字段）
   - `school_registry` - 学校注册表
   - `basic_area_standards` - 基础面积标准
   - `subsidized_area_standards` - 补贴面积标准
   - `planned_students` - 规划学生数
   - `special_subsidies` - 特殊补助
   - `current_area_presets` - 现有面积预设

2. **初始化数据**:
   - 默认管理员账号（admin）
   - 29所上海高校信息
   - 面积标准配置（5类院校 × 7类用房）
   - 规划学生数预设

3. **触发器**:
   - 计算记录删除时级联删除特殊补助

4. **索引优化**:
   - 常用查询字段建立索引
   - 复合索引提升查询性能

---

## 🧪 测试目录详细说明

### 测试文件结构

#### 1. `test/run_all_tests.sh`
**功能**: 测试套件主入口脚本
**用途**: 
- 自动运行所有测试脚本
- 生成测试汇总报告
- 计算测试通过率
- 返回CI/CD友好的退出码

**使用方法**:
```bash
./test/run_all_tests.sh
```

**输出示例**:
```
🧪 运行所有测试套件
====================

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  测试 1/1: 安全功能测试
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[测试输出...]

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  测试总结
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

总测试数: 1
通过: 1
失败: 0
通过率: 100%
```

---

#### 2. `test/test_security.sh`
**功能**: 安全功能自动化测试
**测试项目**:
1. ✅ **Helmet 安全头验证**
   - X-Content-Type-Options
   - X-Frame-Options / CSP
   - Strict-Transport-Security

2. ✅ **登录接口限流测试**（防暴力破解）
   - 发送 6 次登录请求
   - 前 5 次应返回 401（认证失败）
   - 第 6 次应返回 429（限流触发）

3. ✅ **API 通用限流测试**
   - 验证 100次/15分钟限制
   - 快速发送 3 次请求（不触发）

4. ✅ **下载接口限流测试**（防数据爬取）
   - 自动登录获取 Session Cookie
   - 发送 11 次下载请求
   - 前 10 次应返回 200/404
   - 第 11 次应返回 429（限流触发）

5. ✅ **计算接口限流测试**（防资源滥用）
   - 使用登录状态发送请求
   - 发送 21 次计算请求
   - 前 20 次正常，第 21 次限流

**智能功能**:
- 自动检测服务器运行状态
- 提示重启服务器以清除限流
- 自动登录并管理 Session Cookie
- 测试完成后自动清理临时文件

**使用方法**:
```bash
./test/test_security.sh
```

**前置条件**:
- 服务器运行中（`npm start` 或 `pm2 start`）
- 端口 3000 可访问
- 建议测试前重启服务器清除限流记录

---

#### 3. `test/README.md` (232行)
**功能**: 测试文档和使用指南
**内容包括**:
- 测试套件说明
- 运行方法和最佳实践
- 测试前准备和测试后清理
- 未来扩展计划（单元测试、集成测试、E2E测试）
- 常见问题解答

---

#### 4. `test/QUICK_REFERENCE.md`
**功能**: 测试快速参考卡片
**内容包括**:
- 快速开始指南
- 测试前检查清单
- 当前测试覆盖表格
- 日志查看命令
- 常见问题速查
- 预期测试结果示例

---

### 测试覆盖范围

| 测试类型 | 状态 | 覆盖率 | 说明 |
|---------|------|--------|------|
| **安全测试** | ✅ 已实现 | 100% | Helmet + Rate Limiting |
| 单元测试 | ⏳ 计划中 | 0% | 认证、计算、数据库模块 |
| 集成测试 | ⏳ 计划中 | 0% | API 接口和业务流程 |
| 性能测试 | ⏳ 计划中 | 0% | 负载和压力测试 |
| E2E 测试 | ⏳ 计划中 | 0% | 完整用户流程 |

---

### 测试日志和监控

**查看限流触发日志**:
```bash
# 使用 pm2
pm2 logs school-area-system | grep "限流触发"

# 查看日志文件
tail -f logs/combined.log | grep "限流触发"

# 统计今日限流次数
grep "限流触发" logs/combined.log | grep "$(date +%Y-%m-%d)" | wc -l
```

**日志格式示例**:
```
[2025-10-22T10:30:45.123Z] 限流触发 - 登录限流（可能的暴力破解尝试）
  IP: 192.168.1.100
  用户: 未登录
  路径: /api/auth/login
  User-Agent: Mozilla/5.0...
```

---

## 📦 数据与备份目录

### backups/ 目录结构

#### database/ - 数据库备份
**包含**:
- `backup_20250905T052821/` - 2025年9月5日数据库备份
- `backup_20250905T052857/` - 2025年9月5日数据库备份

**每个备份包含**:
- `basic_area_standards.sql` - 基础面积标准
- `calculation_history.sql` - 计算历史记录
- `school_registry.sql` - 学校注册表
- `special_subsidies.sql` - 特殊补助数据
- `subsidized_area_standards.sql` - 补贴面积标准
- `users.sql` - 用户数据

**恢复方法**:
```bash
cd backups/database/backup_20250905T052857/
mysql -u root -p school_area_db < basic_area_standards.sql
mysql -u root -p school_area_db < calculation_history.sql
# ... 其他表
```

---

#### frontend_old/ - 前端旧版本备份（已废弃）
**说明**: 包含2025年8月14日之前的前端代码备份
- 模块化重构前的单文件版本
- CSS 和 JavaScript 重构前的备份
- 已被当前模块化架构替代

⚠️ **注意**: 这些备份仅作历史参考，不建议恢复使用

---

### logs/ 目录
**运行日志存储**:
- `combined.log` - 综合日志（包含所有级别）
- `error.log` - 错误日志（仅错误和警告）

**日志查看**:
```bash
# 实时查看所有日志
tail -f logs/combined.log

# 查看错误日志
tail -f logs/error.log

# 搜索特定内容
grep "限流触发" logs/combined.log
```

---

### output/ 目录
**导出文件存储**:
- Excel导出文件临时存储
- 批量导出结果
- 在线测算结果

⚠️ **注意**: 此目录文件会定期清理，不建议长期存储

---

## ⚙️ 配置文件说明

### .env（环境变量）
**不提交到Git**，包含敏感信息

**必需配置**:
```env
# 数据库配置
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=school_area_db
DB_PORT=3306

# 服务器配置
PORT=3000
SESSION_SECRET=your-secret-key-here

# HTTPS配置（可选）
HTTPS_ENABLED=false
HTTPS_FORCE_REDIRECT=false
```

---

### .env.example（环境变量模板）
**提交到Git**，供开发者参考

**使用方法**:
```bash
cp .env.example .env
# 然后编辑 .env 填入实际配置
```

---

### .gitignore
**Git忽略规则**

**主要忽略**:
- `.env` - 环境变量文件
- `node_modules/` - 依赖目录
- `*.log` - 日志文件
- `logs/` - 日志目录
- `backups/` - 备份文件
- `output/` - 输出文件
- `cookies.txt` - Cookie文件
- `.DS_Store` - macOS系统文件

---

## 🔧 开发工具和脚本

### install.sh
**功能**: 自动化安装脚本
**内容**:
- 检查Node.js版本
- 安装npm依赖
- 数据库配置向导
- .env文件生成
- 首次启动指南

**使用方法**:
```bash
chmod +x install.sh
./install.sh
```

---

### scripts/seed_users.js
**功能**: 批量创建用户脚本
**用途**:
- 快速创建多个学校账号
- 测试环境用户生成
- 批量导入用户数据

**使用方法**:
```bash
node scripts/seed_users.js
```

---

## 📊 依赖管理

### package.json
**主要依赖**:

**运行时依赖**:
```json
{
  "express": "^4.18.2",
  "mysql2": "^3.6.0",
  "bcrypt": "^5.1.1",
  "express-session": "^1.17.3",
  "helmet": "^7.1.0",
  "express-rate-limit": "^7.1.5",
  "cors": "^2.8.5",
  "xlsx": "^0.18.5"
}
```

**开发依赖**:
```json
{
  "nodemon": "^3.0.1"
}
```

**脚本命令**:
```json
{
  "start": "node server.js",
  "dev": "nodemon server.js"
}
```

---

## 🗂️ 数据模型

### 数据库表关系

```
users (用户表)
    ↓
calculation_history (计算历史)
    ↓
special_subsidies (特殊补助)

school_registry (学校注册表)
    ↓
planned_students (规划学生数)
    ↓
current_area_presets (现有面积预设)

basic_area_standards (基础标准)
subsidized_area_standards (补贴标准)
```

### 核心字段说明

#### calculation_history (77个字段)
**学生数据** (8个字段):
- fulltime_undergrad - 全日制本科生
- fulltime_specialist - 全日制专科生
- fulltime_master - 全日制硕士生
- fulltime_doctor - 全日制博士生
- international_undergrad - 留学生本科
- international_specialist - 留学生专科
- international_master - 留学生硕士
- international_doctor - 留学生博士

**面积数据** (7种用房类型 × 7个维度 = 49个字段):

用房类型:
1. teaching - 教学及辅助用房
2. library - 图书馆
3. sports - 体育用房
4. admin - 行政办公用房
5. life - 生活福利及其他用房
6. special - 专项
7. infrastructure - 附属用房（地下车库等）

每种用房类型的7个维度:
- current - 现有面积
- preliminary - 初步规划面积
- under_construction - 在建面积
- planned - 规划面积
- total - 汇总面积（自动计算）
- required - 应配面积（自动计算）
- gap - 缺口面积（自动计算）

---

## 🔍 常见开发任务

### 添加新的面积字段
1. 修改 `scripts/db.sql` 中的表结构
2. 更新 `config/dataService.js` 中的字段映射
3. 修改前端表单 `public/html/data-entry.html`
4. 更新计算逻辑 `public/js/dataEntry.js`
5. 调整Excel导出格式 `server.js`

### 添加新的用户角色
1. 修改 `users` 表的 `role` 字段枚举
2. 更新 `config/authService.js` 中的角色验证
3. 添加新的中间件（如 `requireNewRole`）
4. 更新前端权限控制逻辑

### 修改面积标准
1. 方式一：通过管理员界面（推荐）
   - 登录→标准管理→修改标准值

2. 方式二：直接修改数据库
   ```sql
   UPDATE basic_area_standards 
   SET standard_value = 新值 
   WHERE school_type = '院校类型' AND area_type = '用房类型';
   ```

### 调试数据持久化问题
1. 检查 `config/dataService.js` 中的字段映射
2. 查看服务器日志中的SQL语句
3. 验证前端发送的数据格式
4. 确认数据库表结构和字段名

---

## 🚨 常见问题排查

### 问题1: 数据保存后查询不到
**排查步骤**:
1. 检查控制台Network标签，确认API响应
2. 查看`logs/combined.log`中的数据库操作日志
3. 直接查询数据库确认数据是否插入成功
4. 验证字段名映射（参考dataService.js）

### 问题2: Excel导出显示0值
**排查步骤**:
1. ✅ 已修复（2025年1月）
2. 确认使用最新版本代码
3. 验证server.js中使用`*_total`字段
4. 查询数据库确认数据存在

### 问题3: 下载操作报错
**排查步骤**:
1. ✅ 已修复（2025年1月）
2. 确认使用最新版本api.js
3. 验证apiDelete函数有`data &&`检查
4. 检查浏览器控制台错误信息

### 问题4: 登录限流触发
**解决方法**:
```bash
# 重启服务器清除限流记录
pm2 restart school-area-system

# 或等待 15 分钟后重试
```

### 问题5: 测试失败
**排查步骤**:
1. 确认服务器运行中（`pm2 status`）
2. 检查数据库连接
3. 重启服务器清除限流
4. 查看测试日志详细错误

---

## 📚 扩展资源

### 官方文档链接
- [Node.js文档](https://nodejs.org/docs/)
- [Express.js文档](https://expressjs.com/)
- [MySQL文档](https://dev.mysql.com/doc/)
- [BCrypt文档](https://github.com/kelektiv/node.bcrypt.js)
- [XLSX.js文档](https://docs.sheetjs.com/)
- [Helmet文档](https://helmetjs.github.io/)
- [express-rate-limit文档](https://github.com/express-rate-limit/express-rate-limit)

### 学习资源
- RESTful API设计最佳实践
- Node.js安全编程指南
- MySQL性能优化指南
- 前端模块化架构设计

---

<div align="center">

**本文档最后更新**: 2025年10月22日  
**项目版本**: v2.1.1

</div>
